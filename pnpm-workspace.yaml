packages:
  - "packages/*"
  - "examples/static-html"
  - "examples/full-stack-local"
  # react-consumer-example is intentionally excluded — it uses the published npm
  # package and should run independently via npm install / npm run dev
