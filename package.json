{
  "name": "ai-chat-toolkit",
  "private": true,
  "version": "0.1.0",
  "description": "Embeddable AI chat micro frontend toolkit",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "dev:widget": "pnpm --filter ai-chat-toolkit-widget dev",
    "dev:server": "pnpm --filter ai-chat-toolkit-server dev"
  },
  "engines": {
    "node": ">=18"
  }
}
