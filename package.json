{
  "name": "ai-chat-toolkit",
  "private": true,
  "version": "0.1.0",
  "description": "Embeddable AI chat micro frontend toolkit",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @ai-chat-toolkit/widget dev",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=18"
  }
}
